# Ayham — Personal Site

Static HTML site. To deploy on GitHub Pages:

1. Create a new GitHub repo
2. Upload these files to the root
3. Go to **Settings → Pages**, set Source to `main` branch, `/root`
4. Visit `https://<your-username>.github.io/<repo-name>/`

Open `index.html` locally in any browser to preview.
